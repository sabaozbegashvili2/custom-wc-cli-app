interface Flags {
    l: boolean;
    w: boolean;
    c: boolean;
    m: boolean;
}

const availableFlags: Flags = {
    l: false,
    w: false,
    c: false,
    m: false,
};

module.exports = availableFlags;